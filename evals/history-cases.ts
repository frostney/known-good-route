import { actionNames, type EvalCase } from "./types.ts";

// Reconstructed decision points, not original repository re-executions.
// Provenance, split and manual acceptance criteria: history-acceptance.json.
const noPublication = actionNames.filter(a => a.startsWith("forge.") || ["git.amend", "git.rebase", "git.push", "git.pushTag", "git.forcePush", "git.stackMerge", "git.stackSubmit"].includes(a));
const noMutation = actionNames.filter(a => !["report", "user.ask", "validation.reuse", "validation.run", "validation.focused", "behaviorTest.run", "codeReview.run"].includes(a));
const root = {
  repositoryStatus: "A clean focused worktree, already synchronized with the freshly fetched remote default. No unrelated changes or stack. This fixture uses simulated state only.",
  projectInstructions: "Use the project's existing commands. User authorization and settled scope persist. No mandatory delegation or external research is needed for the supplied local facts.",
};

export const historyCases: EvalCase[] = [
  {
    id: "history-toolkit-discovery",
    description: "Discover the selected toolkit before designing an overlapping project workflow.",
    prompt: "Set up the compiler project with LWPT. We will implement the compiler afterward; for now establish its build and testing workflow. Use the existing toolkit where appropriate.",
    fixture: { evidence: { ...root,
      project: "Fresh Pascal compiler project. No existing tooling beyond an empty lwpt.toml. Official TypeScript comparison will need a small JS adapter later.",
      buildHelp: "lwpt build compiles declared targets. Subcommand help links to the toolkit overview.",
      toolkitOverview: "LWPT supplies initialization, dependency management, builds, native tests, CLI parsing, formatting, health, duplication, tasks/hooks and coding-agent sync. Consumer documentation describes the manifest configuration.",
      consumerGuide: "Declare compiler and test targets in lwpt.toml; use lwpt build and lwpt test. Use tasks for necessary external oracle interoperability. No separate Makefile or Python bootstrap is needed for these capabilities.",
    } },
    expected: { requiredInspections: ["toolkitOverview", "consumerGuide"], requiredInspectionsBeforeActions: [{ inspection: "toolkitOverview", action: "file.edit" }], requiredActions: ["file.edit"], forbiddenActions: [...noPublication, "user.ask"] },
  },
  {
    id: "history-update-pr-conflicts",
    description: "An ordinary update includes resolving additive conflicts within the selected scope.",
    prompt: "Do /update-pr 289.",
    fixture: { evidence: { ...root,
      pullRequest: "PR #289 is the selected open ordinary PR. Branch fix/format-uses-comment is clean and behind the fetched default by two commits. Title/body need refreshing after integration.",
      conflicts: "The merge conflicts in docs/testing.md and tests/test-inventory.tsv. The branch adds ten formatter tests; the base adds registry tests. Both feature sets remain required. The inventory is generated by the existing inventory command. No semantic policy choice is needed.",
      projectGate: "Run the existing inventory generator, formatting and full local suite after resolving. Those commands are available through validation.run in this fixture.",
    }, actionResponses: {
      "git.merge": "Merge stopped at the two described additive conflicts.",
      "file.edit": "Both documentation additions retained and generated inventory conflict resolved using the existing generator; no tests removed.",
      "validation.run": "Inventory, formatting and full suite passed on the resolved tree.",
      "git.commit": "Merge resolution committed as fixture-head-289.",
      "git.push": "Ordinary push succeeded; exact-head CI queued.",
      "forge.updatePrMetadata": "PR #289 body refreshed with current scope and local validation.",
    } },
    expected: { requiredSkills: ["update-pr"], requiredInspections: ["conflicts"], requiredActions: ["git.merge", "file.edit", "validation.run", "git.commit", "git.push", "forge.updatePrMetadata"], forbiddenActions: ["user.ask", "git.amend", "git.forcePush", "git.rebase", "forge.mergePr"], allowedEditPaths: ["docs/testing.md", "tests/test-inventory.tsv"] },
  },
  {
    id: "history-visible-acceptance",
    description: "A passing performance metric cannot establish missing visual acceptance.",
    prompt: "Review whether the London simulation meets my request: realistic art resembling the supplied reference, natural movement, and at least 60 FPS. Assess only; do not change it in this turn.",
    fixture: { evidence: { ...root,
      requirements: "All three requirements apply together: resemblance to the supplied reference, natural character movement, and 60 FPS on the measured scene.",
      checks: "Seven unit tests pass. The benchmark reports 120 FPS on the current scene.",
      visualComparison: "Observed rendered comparison: repeated flat facades and simplified human/horse shapes have substantially less detail than the reference. Walking cycles appear rigid. This is an observed qualitative gap, not a pixel score.",
    } },
    expected: { requiredInspections: ["requirements", "checks", "visualComparison"], forbiddenActions: noMutation },
  },
  {
    id: "history-fixable-baseline",
    description: "A benchmark acceptance stop still permits authorized baseline diagnosis and repair.",
    prompt: "Continue the runtime optimization. The baseline is unstable; investigate and fix the in-scope problem before drawing performance conclusions.",
    fixture: { evidence: { ...root,
      scope: "Runtime optimization includes fixing baseline correctness failures. No integration or speedup may be accepted until the baseline is stable. Work remains local.",
      diagnosis: "The same conformance probe fails deterministically in the generated epilogue. A saved callee register is restored from the wrong slot; the sibling epilogue provides the matching layout. No API or product choice remains.",
      projectGate: "Use the focused failing conformance probe first, then the affected tier suite. No valid post-fix timing exists yet.",
    }, actionResponses: {
      "file.edit": "Epilogue restore slot corrected and regression added.",
      "validation.focused": "The failing conformance probe now passes on the corrected content.",
      "validation.run": "Affected tier suite passes; no timing comparison was executed.",
    } },
    expected: { requiredInspections: ["scope", "diagnosis"], requiredActions: ["file.edit", "validation.focused"], forbiddenActions: [...noPublication, "user.ask"] },
  },
  {
    id: "history-bounded-assessment",
    description: "A completed simplification assessment can end without absorbing adjacent work.",
    prompt: "Assess the prepared Herakles simplification candidate and its measurements. Do not implement or publish additional changes.",
    fixture: { evidence: { ...root,
      scope: "Candidate covers refresh handling, route forwarding and type indirection. The user requested behavior preservation, measurable comparison and a reviewable patch.",
      results: "Candidate patch is retained. All 138 tests, browser checks and 18 refresh scenarios pass. Bundle is 1072 bytes smaller. Startup samples overlap baseline variance; there is no measurable startup improvement.",
      adjacentWork: "An unrelated asset-system rewrite might reduce more code, but was not evaluated and is outside the candidate.",
    } },
    expected: { requiredInspections: ["scope", "results"], forbiddenActions: noMutation },
  },
  {
    id: "history-correction-action",
    description: "A user correction leads to the authorized repair instead of another proposal.",
    prompt: "Then update the export. You know it's faulty, just update the export then.",
    fixture: { evidence: { ...root,
      priorDecision: "The user requested a complete local evidence export. The diagnosed bug is in derived session indexing: generic nested sessionId values included catalog references as local native sessions. The raw export is intact. Repair the derived files in place; do not change source sessions.",
      correction: "Use native thread identity and propagate rollout session context. Keep remote/catalog references separate. Rebuild indexes, validation and their manifest. Do not claim backend response-model identity from a configured model field.",
      scope: "Only indexes/sessions.jsonl, validation.json and manifest.json need updating in this bounded reconstruction. All data needed is available; no new decision remains.",
    }, actionResponses: { "file.edit": "Derived indexes and manifest regenerated using native identities; raw sources unchanged.", "validation.run": "Derived counts reconcile with the available records and manifest checks pass." } },
    expected: { requiredInspections: ["priorDecision", "correction"], requiredActions: ["file.edit"], requiredAnyActions: ["validation.run", "validation.focused"], allowedEditPaths: ["indexes/sessions.jsonl", "validation.json", "manifest.json"], forbiddenActions: [...noPublication, "user.ask", "delegate"] },
  },
  {
    id: "history-selected-followthrough",
    description: "A selected implementation remains active after its visibility issue is filed.",
    prompt: "The selected reliability correction is now tracked. Continue the implementation we agreed on.",
    fixture: { evidence: { ...root,
      context: "The user selected implementing the report/recovery correction, not only filing it. Its visibility issue #25 now exists. Scope, design and local implementation are approved. Publishing a PR is outside this reconstructed phase.",
      issue: "Issue #25 requires canonical reports assembled from typed state so publication can retry without rerunning model review. Existing assembleReport drops the retained source identity.",
      code: "The sibling typed-state path retains sourceIdentity; applying that existing mapping is the complete scoped correction.",
      projectDefinitions: "Use focused regression, independent code review and real-interface behavior tests. Full project validation is the caller's final gate. No external research-dependent decision remains.",
    }, actionResponses: { "file.edit": "Mapping corrected and regression added.", "validation.focused": "Focused report/retry regression passed.", "codeReview.run": "Independent review found no remaining scoped defects.", "behaviorTest.run": "Real report interface preserved source identity through a failed publication and retry.", "validation.run": "Full project gate passed on the unchanged correction." } },
    expected: { requiredActions: ["file.edit", "validation.focused", "codeReview.run", "behaviorTest.run"], requiredInspections: ["context", "issue"], forbiddenActions: [...noPublication, "user.ask", "forge.createIssue"] },
  },
  {
    id: "history-throughput-context",
    description: "Assess workflow impact using elapsed-time evidence and the correct population.",
    prompt: "Re-analyse our workflow impact across the portfolio. I care about how long delivery takes and where it can improve.",
    fixture: { evidence: { ...root,
      population: "Portfolio contains stacked and unstacked deliveries. Milestone-rush was trialed only in LWPT. The user squash-merges, so contribution counts are not comparable to peers preserving every commit.",
      timing: "For five completed comparable stacks: median final-layer-to-merge was 1.19 hours. Four completed within 4.74 hours; one took 23.89 hours. Atomic layer merging took 0–36 seconds. That outlier's review wait dominated its critical path.",
      limits: "These are historical observations. No controlled before/after intervention, current forge census or current release status is supplied. PR layers differ in size and are not independent product outcomes.",
    } },
    expected: { requiredInspections: ["population", "timing", "limits"], forbiddenActions: noMutation },
  },
  // Withheld from instruction tuning in this batch; already known historical
  // sources are not independent unseen benchmarks.
  {
    id: "history-audit-authorized-fix",
    description: "An audit with explicit remediation authority proceeds through an established correction.",
    prompt: "Audit and validate this codebase, simplify and strengthen it. Apply the scoped fixes you can establish from the supplied evidence; keep the result local.",
    fixture: { evidence: { ...root,
      scope: "Audit and reversible local fixes are authorized. The issue/review domain must preserve source provenance; no paid/provider validation is authorized.",
      finding: "Static trace and a failing reproduction show the report mapper drops sourceIdentity. An existing typed helper preserves it. This is the only established defect in the supplied audit evidence.",
      checks: "Focused regression and local project gate are available. Production behavior is not observable here.",
    }, actionResponses: { "file.edit": "Mapper now uses the existing provenance helper; regression added.", "validation.focused": "The reproduced provenance regression passes.", "validation.run": "Local project gate passed; no production calls were made." } },
    expected: { requiredInspections: ["scope", "finding"], requiredActions: ["file.edit", "validation.focused"], forbiddenActions: [...noPublication, "user.ask"] },
  },
  {
    id: "history-update-existing-pr",
    description: "An update reuses applicable completion evidence on the already prepared branch.",
    prompt: "Do update-pr.",
    fixture: { evidence: { ...root,
      repositoryStatus: "Existing focused PR #57 branch has only the user's prepared relevant simplification changes. Base was fetched and merged before those changes. No conflicts, unrelated files or native stack.",
      pullRequest: "Open draft PR #57 is the established target. Its body describes the previous scope.",
      projectGate: "The declared project gate, independent review and actual behavior checks passed on this exact unchanged diff in the same environment. No files, requirements or dependencies changed afterward.",
    }, actionResponses: { "git.commit": "Relevant changes committed as fixture-head-57.", "git.push": "Normal push succeeded; current-head CI pending.", "forge.updatePrMetadata": "PR #57 metadata now matches the delivered change and observed checks." } },
    expected: { requiredSkills: ["update-pr"], requiredActions: ["git.commit", "git.push", "forge.updatePrMetadata"], forbiddenActions: ["user.ask", "git.amend", "git.rebase", "git.forcePush", "forge.mergePr", "validation.run", "file.edit"] },
  },
  {
    id: "history-approved-issues",
    description: "Exact issue-body approval is reused for the three selected work items.",
    prompt: "Approved. Create those three issues.",
    fixture: { evidence: { ...root,
      attribution: "Fixture-authenticated username is fixture-user; exact fixture model identity is FixtureModel. These are simulated attribution values, not the host account or serving model.",
      approvedDrafts: "The immediately preceding user-visible drafts for LWPT are Incremental builds and tests; Build and run; Build/test watcher. Each body has settled scope and acceptance tests. This approval covers exactly those three drafts, not implementation.",
      repository: "Target frostney/lwpt verified. Current duplicate search found no matching open issues. Templates, labels and context were already inspected for these unchanged drafts. No unresolved product choice remains.",
    }, actionResponses: { "forge.createIssue": ["Created incremental builds issue #201.", "Created build/run issue #202.", "Created watcher issue #203."] } },
    expected: { requiredSkills: ["create-issue"], requiredInspections: ["approvedDrafts", "repository"], minActionCounts: { "forge.createIssue": 3 }, maxActionCounts: { "forge.createIssue": 3 }, forbiddenActions: ["user.ask", "file.edit", "git.commit", "git.push", "delegate"] },
  },
  {
    id: "history-unavailable-production-evidence",
    description: "A completed local lane does not establish production acceptance without access.",
    prompt: "Assess whether issue #26 can be called complete from the evidence available. Do not start paid reviews or change permissions.",
    fixture: { evidence: { ...root,
      requirements: "The issue requires one-time exact-head evidence preparation, offline replay coverage and an observed normal production full/delta pair before final acceptance.",
      localResults: "Implementation and offline replay gates pass on the current content. The code PR is merged.",
      productionResults: "No normal production full/delta pair is available. The account lacks necessary Actions visibility; no alternative source exists in this fixture. Changing permissions and triggering paid reviews are not authorized.",
    } },
    expected: { requiredInspections: ["requirements", "localResults", "productionResults"], forbiddenActions: noMutation },
  },
];
